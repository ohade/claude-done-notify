# claude-done-notify configuration
# Copy this file to ~/.claude-done-notify.env and fill in your values.
# chmod 600 ~/.claude-done-notify.env

# Required: Slack bot OAuth token (needs chat:write scope)
SLACK_BOT_TOKEN="xoxb-your-token-here"

# Required: Slack channel or DM ID to send notifications to
# Find your DM channel ID: open Slack in browser, click on the DM,
# the channel ID is in the URL (e.g., D01234ABCDE)
SLACK_CHANNEL="D01234ABCDE"

# Optional: Minimum turn duration (seconds) before notifying (default: 10)
# CDN_MIN_DURATION=10

# Optional: Rate limit between notifications per session in seconds (default: 60)
# CDN_COOLDOWN=60

# Optional: Seconds to wait before checking focus — avoids race condition (default: 2)
# CDN_FOCUS_DELAY=2

# Optional: Terminal detection mode (default: auto)
#   auto    — detect WezTerm if available, fall back to generic
#   wezterm — force WezTerm pane-level detection
#   generic — app-level detection only (any terminal)
#   none    — skip focus detection entirely (always notify)
# CDN_TERMINAL=auto

# Optional: Directory for rate-limit marker files (default: ~/.claude/session-signals)
# CDN_SIGNALS_DIR="${HOME}/.claude/session-signals"

# Optional: Debug log file path (default: ~/.claude/hooks/debug-claude-done-notify.log)
# CDN_LOG_FILE="${HOME}/.claude/hooks/debug-claude-done-notify.log"
